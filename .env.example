VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_POWERSYNC_URL=

# Copy the "Direct connection" URI from Supabase -> Connect.
# For the fastest dev setup, you can use that URI as-is.
# For a hardened setup, replace the username/password in that URI with a dedicated
# replication user such as `powersync_role`.
PS_DATABASE_URI=
