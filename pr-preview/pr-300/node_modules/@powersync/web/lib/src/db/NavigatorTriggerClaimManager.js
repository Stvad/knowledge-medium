import { getNavigatorLocks } from "../shared/navigator.js";
//#region node_modules/@powersync/web/lib/src/db/NavigatorTriggerClaimManager.js
/**
* @internal
* @experimental
*/
var NAVIGATOR_TRIGGER_CLAIM_MANAGER = {
	async obtainClaim(identifier) {
		return new Promise((resolveReleaser) => {
			getNavigatorLocks().request(identifier, async () => {
				await new Promise((releaseLock) => {
					resolveReleaser(async () => releaseLock());
				});
			});
		});
	},
	async checkClaim(identifier) {
		return (await getNavigatorLocks().query()).held?.find((heldLock) => heldLock.name == identifier) != null;
	}
};
//#endregion
export { NAVIGATOR_TRIGGER_CLAIM_MANAGER };

//# sourceMappingURL=NavigatorTriggerClaimManager.js.map