import { AbstractStreamingSyncImplementation, LockType } from "../../../../../common/dist/bundle.js";
import { getNavigatorLocks } from "../../shared/navigator.js";
//#region node_modules/@powersync/web/lib/src/db/sync/WebStreamingSyncImplementation.js
var WebStreamingSyncImplementation = class extends AbstractStreamingSyncImplementation {
	constructor(options) {
		super(options);
	}
	get webOptions() {
		return this.options;
	}
	async obtainLock(lockOptions) {
		const identifier = `streaming-sync-${lockOptions.type}-${this.webOptions.identifier}`;
		if (lockOptions.type == LockType.SYNC) this.logger.debug("requesting lock for ", identifier);
		return getNavigatorLocks().request(identifier, { signal: lockOptions.signal }, lockOptions.callback);
	}
};
//#endregion
export { WebStreamingSyncImplementation };

//# sourceMappingURL=WebStreamingSyncImplementation.js.map