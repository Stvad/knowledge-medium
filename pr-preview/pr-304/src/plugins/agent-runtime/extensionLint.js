var e=e=>/token|secret|password|api[_-]?key|credentials?|auth/i.test(e),t=/(?:window\.)?localStorage\s*\.\s*setItem\s*\(\s*([^,)]+)/,n=/^\s*['"]([^'"]+)['"]\s*$/,r=/window\s*\.\s*dispatchEvent\s*\(\s*new\s+CustomEvent\s*(?:<[^>]*>)?\s*\(\s*([^,)]+)/,i=/open|toggle|dialog|picker|prompt|modal/i,a=[{rule:`config-in-localstorage`,catalogPattern:`user-prefs-config`,message:"Non-credential settings stored in localStorage. Use `getPluginPrefsBlock(repo, workspaceId, user, type)` so settings sync across the user's devices and benefit from typed property codecs. Keep credentials (tokens, API keys) in localStorage; everything else goes in a prefs block.",testLine(r){let i=r.match(t);if(!i)return null;let a=(i[1]??``).trim().match(n);return a&&e(a[1])?null:i[0]}},{rule:`stored-plugin-block-id`,catalogPattern:`plugin-root-singleton`,message:"Persisting a plugin's root or per-record block id (e.g. in localStorage or a config block) means a cache clear or fresh device creates a duplicate. Derive ids deterministically with `pluginBlockId(workspaceId, NAMESPACE, key)` — same inputs always return the same id, so re-installs land on the existing block.",testLine(e){let t=e.match(/(?:window\.)?localStorage\s*\.\s*setItem\s*\(\s*['"][^'"]*(?:block[_-]?id|root[_-]?id|plugin[_-]?id)[^'"]*['"]/i);return t?t[0]:null}},{rule:`dialog-via-window-event`,catalogPattern:`settings-dialog`,message:"Opening or toggling a dialog by dispatching a `window` CustomEvent (and listening for it with `window.addEventListener` inside the component) reimplements the typed dialog channel over an untyped string bus. For a one-shot prompt, `openDialog(Component, props)` returns a promise that resolves with the user's choice. For a persistent toggle surface, drive visibility from a module store read via `useSyncExternalStore` (the same mechanism the app's own DialogHost uses) and flip it directly from your action's handler. Reserve `window` CustomEvents for genuine broadcast.",testLine(e){let t=e.match(r);if(!t)return null;let n=(t[1]??``).trim();return i.test(n)?t[0]:null}}],o=/\/\/\s*lint-ok\s*:\s*([\w-]+)/,s=e=>{let t=new Set;for(let n of e.split(`
`)){let e=n.match(o);e?.[1]&&t.add(e[1])}return t},c=e=>{if(!e)return[];let t=s(e),n=[],r=e.split(`
`);for(let e of a)if(!t.has(e.rule))for(let t of r){let r=e.testLine(t);if(r){n.push({rule:e.rule,message:e.message,catalogPattern:e.catalogPattern,example:r.length>120?`${r.slice(0,117)}...`:r});break}}return n.sort((e,t)=>e.rule.localeCompare(t.rule)),n};export{c as lintExtensionSource};
//# sourceMappingURL=extensionLint.js.map