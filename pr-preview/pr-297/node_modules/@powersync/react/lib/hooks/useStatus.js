import { usePowerSyncStatus } from "./deprecated/usePowerSyncStatus.js";
//#region node_modules/@powersync/react/lib/hooks/useStatus.js
/**
* Custom hook that provides access to the current status of PowerSync.
* @returns The PowerSync Database status.
* @example
* import { useStatus } from "@powersync/react";
*
* const Component = () => {
*   const status = useStatus();
*
*   return <div>
*     status.connected ? 'wifi' : 'wifi-off'
*   </div>
* };
*/
var useStatus = () => usePowerSyncStatus();
//#endregion
export { useStatus };

//# sourceMappingURL=useStatus.js.map