import React from "react";
//#region node_modules/@powersync/react/lib/hooks/PowerSyncContext.js
var PowerSyncContext = React.createContext(null);
/**
* Custom hook that provides access to the PowerSync context.
* @returns The PowerSync Database instance.
* @example
* const Component = () => {
*   const db = usePowerSync();
*   const [lists, setLists] = React.useState([]);
*
*   React.useEffect(() => {
*     powersync.getAll('SELECT * from lists').then(setLists)
*   }, []);
*
*   return <ul>
*     {lists.map(list => <li key={list.id}>{list.name}</li>)}
*   </ul>
* };
*/
var usePowerSync = () => React.useContext(PowerSyncContext);
//#endregion
export { PowerSyncContext, usePowerSync };

//# sourceMappingURL=PowerSyncContext.js.map