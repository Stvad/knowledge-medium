import{propertyNameProp as e}from"../../data/properties.js";import{PROPERTY_SCHEMA_TYPE as t}from"../../data/blockTypes.js";var n=50,r=25,i=3,a=`json_valid(b.properties_json) AND json_type(b.properties_json) = 'object'`,o=`CASE WHEN ${a} THEN b.properties_json ELSE '{}' END`,s=`NOT (${a})`,c="Nothing declares this name — no definition block, no code seed — so property migration skips it (propertyChildrenProcessor.ts: `resolveNameSchema` → `continue`) and it is the one class of property data a flipped workspace cannot make child-backed. Fix IN THIS ORDER: (1) if an extension owns the key, install / enable it so its seed materializes the definition; (2) only then let §9 orphan synthesis mint a user-origin definition. Synthesizing first and enabling the owner later collides in the winner machinery and strands field rows on the loser's fieldId. Registering is additive — no cell value is rewritten.",l="A definition block exists for this name but the workspace cannot build behavior from it, so it registers no schema and the name resolves to nothing. Check IN THIS ORDER: (1) is its preset contributed by an extension that is not installed / enabled / loaded here? `tryBuildSchema` returns nothing for an unknown preset id, and the definition is then perfectly valid — enabling the provider fixes it, while editing the block would destroy a working preset config; (2) only if the provider IS loaded, repair the block itself (config the codec rejects, or metadata that fails to parse). Either way do not add a second definition for this name — it would collide.",u=`The empty property key is a hard flip blocker: no definition can ever back it. Delete or remap it before any workspace flips to child-backed properties (§9).`,d=e=>e.property===``?{fix:u,blocksFlip:!0}:e.reason===`ambiguous`?{fix:"Unreachable via a name lookup (see describeUnregisteredProperty). Two code seeds declaring one name would leave only the first in the registry; namespace one of them (`myplugin:name`)."}:e.reason===`shadowed`?{fix:`Unreachable via a name lookup (see describeUnregisteredProperty). A non-winning definition block for this name would be shadowed by the winner; retire or rename the loser rather than adding another definition.`}:{fix:e.definitionBlocks>0?l:c},f=e=>{if(!e)return[];try{let t=JSON.parse(e);return Array.isArray(t)?t.filter(e=>typeof e==`string`):[]}catch{return[]}},p=e=>typeof e==`string`?e:String(e??``),m=async(n,r,i={})=>{let a=()=>{let e=n.propertyDefinitions;if(!e||e.workspaceId!==r)throw Error(`Cannot audit ${r}: its property-definition registry is not loaded (loaded: ${e?.workspaceId??`none`}). Classification would read every key as unregistered. Open that workspace in the app and re-run.`);return e};a(),await n.whenPropertyDefinitionsReady(r);let c=await n.syncViewGap(),l=a(),u=n.propertySchemaResolverFor(r),f=await n.db.getAll(`SELECT j.key AS property, COUNT(*) AS cells
       FROM blocks b, json_each(${o}) j
      WHERE b.workspace_id = ? AND b.deleted = 0
      GROUP BY j.key`,[r]),m=await n.db.get(`SELECT COUNT(*) AS n FROM blocks b
      WHERE b.workspace_id = ? AND b.deleted = 0 AND ${s}`,[r]),g=await n.db.getAll(`SELECT b.id AS id, json_extract(${o}, ?) AS name
       FROM blocks b
       JOIN block_types t ON t.block_id = b.id AND t.workspace_id = b.workspace_id
      WHERE t.type = ? AND b.workspace_id = ? AND b.deleted = 0`,[`$."${e.name}"`,t,r]),_=new Map;for(let e of g){let t=l.definitionsByFieldId.get(e.id)?.name??(typeof e.name==`string`?e.name:void 0);t!==void 0&&_.set(t,(_.get(t)??0)+1)}let v=[],y=0,b=0;for(let e of f){let t=p(e.property);b+=e.cells;let n=u.resolve(t);if(n.status===`resolved`){y+=1;continue}let r=_.get(t)??0;v.push({property:t,cells:e.cells,reason:n.reason,definitionBlocks:r,...d({property:t,reason:n.reason,definitionBlocks:r}),types:[],sampleBlockIds:[]})}return v.sort((e,t)=>t.cells-e.cells||e.property.localeCompare(t.property)),await h(n,r,v,{keys:Math.max(0,Math.floor(i.keys??50)),blocksPerKey:Math.max(1,Math.floor(i.blocksPerKey??25))}),{workspaceId:r,syncGap:c,syncedThrough:n.lastSyncedAt?.toISOString()??null,distinctProperties:f.length,propertyCells:b,registeredProperties:y,unregistered:v,unregisteredCells:v.reduce((e,t)=>e+t.cells,0),unreadableBlocks:m?.n??0}},h=async(e,t,n,r)=>{let a=n.slice(0,r.keys);for(let e of n.slice(r.keys))e.provenanceOmitted=!0;if(a.length===0)return;let s=await e.db.getAll(`SELECT ranked.property AS property,
            ranked.blockId AS blockId,
            (SELECT json_group_array(bt.type) FROM block_types bt
              WHERE bt.block_id = ranked.blockId AND bt.workspace_id = ?) AS types
       FROM (
         SELECT property, blockId,
                ROW_NUMBER() OVER (PARTITION BY property ORDER BY blockId) AS rn
           FROM (
             -- DISTINCT because a stored bag CAN repeat a key: JSON.stringify
             -- cannot produce one, but a raw SQL write can, and the types
             -- trigger sees only one row so nothing rejects it. Without this,
             -- json_each emits a row per occurrence and one block could eat
             -- the whole per-key cap: repeating in sampleBlockIds, inflating
             -- sampledBlocks, and hiding the blocks that actually differ.
             SELECT DISTINCT j.key AS property, b.id AS blockId
               FROM blocks b, json_each(${o}) j
              WHERE b.workspace_id = ? AND b.deleted = 0
                AND j.key IN (SELECT value FROM json_each(?))
           )
       ) ranked
      WHERE ranked.rn <= ?`,[t,t,JSON.stringify(a.map(e=>e.property)),r.blocksPerKey]),c=new Map,l=new Map;for(let e of s){let t=p(e.property),n=l.get(t)??[];n.length<i&&(n.push(e.blockId),l.set(t,n));let r=c.get(t)??new Map;for(let t of f(e.types))r.set(t,(r.get(t)??0)+1);c.set(t,r)}for(let e of a)e.sampleBlockIds=l.get(e.property)??[],e.types=[...c.get(e.property)??new Map].map(([e,t])=>({type:e,sampledBlocks:t})).sort((e,t)=>t.sampledBlocks-e.sampledBlocks||e.type.localeCompare(t.type))};export{r as PROVENANCE_BLOCKS_PER_KEY,n as PROVENANCE_KEY_LIMIT,m as auditPropertyRegistration,d as describeUnregisteredProperty};
//# sourceMappingURL=propertyRegistrationAudit.js.map