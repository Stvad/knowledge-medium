import{propertyNameProp as e}from"../../data/properties.js";import{PROPERTY_SCHEMA_TYPE as t}from"../../data/blockTypes.js";var n=50,r=25,i=3,a=`CASE WHEN json_valid(b.properties_json) AND json_type(b.properties_json) = 'object'
                        THEN b.properties_json ELSE '{}' END`,o=`NOT (json_valid(properties_json) AND json_type(properties_json) = 'object')`,s="Nothing declares this name — no definition block, no code seed — so property migration skips it (propertyChildrenProcessor.ts: `resolveNameSchema` → `continue`) and it is the one class of property data a flipped workspace cannot make child-backed. Fix IN THIS ORDER: (1) if an extension owns the key, install / enable it so its seed materializes the definition; (2) only then let §9 orphan synthesis mint a user-origin definition. Synthesizing first and enabling the owner later collides in the winner machinery and strands field rows on the loser's fieldId. Registering is additive — no cell value is rewritten.",c=`A definition block exists for this name but the workspace cannot build behavior from it (unknown preset, config the codec rejects, or metadata that fails to parse), so it registers no schema and the name resolves to nothing. Repair that definition block — adding a second one for the same name would collide.`,l=`The empty property key is a hard flip blocker: no definition can ever back it. Delete or remap it before any workspace flips to child-backed properties (§9).`,u=e=>e.property===``?{fix:l,blocksFlip:!0}:e.reason===`ambiguous`?{fix:"Unreachable via a name lookup (see describeUnregisteredProperty). Two code seeds declaring one name would leave only the first in the registry; namespace one of them (`myplugin:name`)."}:e.reason===`shadowed`?{fix:`Unreachable via a name lookup (see describeUnregisteredProperty). A non-winning definition block for this name would be shadowed by the winner; retire or rename the loser rather than adding another definition.`}:{fix:e.definitionBlocks>0?c:s},d=e=>{if(!e)return[];try{let t=JSON.parse(e);return Array.isArray(t)?t.filter(e=>typeof e==`string`):[]}catch{return[]}},f=e=>typeof e==`string`?e:String(e??``),p=async(n,r,i={})=>{let s=n.propertyDefinitions;if(!s||s.workspaceId!==r)throw Error(`Cannot audit ${r}: its property-definition registry is not loaded (loaded: ${s?.workspaceId??`none`}). Classification would read every key as unregistered. Open that workspace in the app and re-run.`);let c=n.propertySchemaResolverFor(r),l=await n.db.getAll(`SELECT j.key AS property, COUNT(*) AS cells
       FROM blocks b, json_each(${a}) j
      WHERE b.workspace_id = ? AND b.deleted = 0
      GROUP BY j.key`,[r]),d=await n.db.get(`SELECT COUNT(*) AS n FROM blocks
      WHERE workspace_id = ? AND deleted = 0 AND ${o}`,[r]),p=await n.db.getAll(`SELECT json_extract(b.properties_json, ?) AS name, COUNT(*) AS n
       FROM blocks b
       JOIN block_types t ON t.block_id = b.id AND t.workspace_id = b.workspace_id
      WHERE t.type = ? AND b.workspace_id = ? AND b.deleted = 0
      GROUP BY name`,[`$."${e.name}"`,t,r]),h=new Map;for(let e of p)typeof e.name==`string`&&h.set(e.name,e.n);let g=[],_=0,v=0;for(let e of l){let t=f(e.property);v+=e.cells;let n=c.resolve(t);if(n.status===`resolved`){_+=1;continue}let r=h.get(t)??0;g.push({property:t,cells:e.cells,reason:n.reason,definitionBlocks:r,...u({property:t,reason:n.reason,definitionBlocks:r}),types:[],sampleBlockIds:[]})}return g.sort((e,t)=>t.cells-e.cells||e.property.localeCompare(t.property)),await m(n,r,g,{keys:i.keys??50,blocksPerKey:i.blocksPerKey??25}),{workspaceId:r,distinctProperties:l.length,propertyCells:v,registeredProperties:_,unregistered:g,unregisteredCells:g.reduce((e,t)=>e+t.cells,0),unreadableBlocks:d?.n??0}},m=async(e,t,n,r)=>{let o=n.slice(0,r.keys);for(let e of n.slice(r.keys))e.provenanceOmitted=!0;if(o.length===0)return;let s=await e.db.getAll(`SELECT ranked.property AS property,
            ranked.blockId AS blockId,
            (SELECT json_group_array(bt.type) FROM block_types bt
              WHERE bt.block_id = ranked.blockId AND bt.workspace_id = ?) AS types
       FROM (
         SELECT j.key AS property,
                b.id AS blockId,
                ROW_NUMBER() OVER (PARTITION BY j.key ORDER BY b.id) AS rn
           FROM blocks b, json_each(${a}) j
          WHERE b.workspace_id = ? AND b.deleted = 0
            AND j.key IN (SELECT value FROM json_each(?))
       ) ranked
      WHERE ranked.rn <= ?`,[t,t,JSON.stringify(o.map(e=>e.property)),r.blocksPerKey]),c=new Map,l=new Map;for(let e of s){let t=f(e.property),n=l.get(t)??[];n.length<i&&(n.push(e.blockId),l.set(t,n));let r=c.get(t)??new Map;for(let t of d(e.types))r.set(t,(r.get(t)??0)+1);c.set(t,r)}for(let e of o)e.sampleBlockIds=l.get(e.property)??[],e.types=[...c.get(e.property)??new Map].map(([e,t])=>({type:e,sampledBlocks:t})).sort((e,t)=>t.sampledBlocks-e.sampledBlocks||e.type.localeCompare(t.type))};export{r as PROVENANCE_BLOCKS_PER_KEY,n as PROVENANCE_KEY_LIMIT,p as auditPropertyRegistration,u as describeUnregisteredProperty};
//# sourceMappingURL=propertyRegistrationAudit.js.map