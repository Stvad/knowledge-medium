import "../../../@codemirror/state/dist/index.js";
import "../../../@codemirror/view/dist/index.js";
import { _extends } from "../../../@babel/runtime/helpers/esm/extends.js";
import { _objectWithoutPropertiesLoose } from "../../../@babel/runtime/helpers/esm/objectWithoutPropertiesLoose.js";
import "../../codemirror-extensions-basic-setup/esm/index.js";
import "../../../@codemirror/theme-one-dark/dist/index.js";
import "./theme/light.js";
import "./getDefaultExtensions.js";
import "./utils.js";
import { useCodeMirror } from "./useCodeMirror.js";
import { forwardRef, useCallback, useImperativeHandle, useRef } from "react";
import { jsx } from "react/jsx-runtime";
//#region node_modules/@uiw/react-codemirror/esm/index.js
var _excluded = [
	"className",
	"value",
	"selection",
	"extensions",
	"onChange",
	"onStatistics",
	"onCreateEditor",
	"onUpdate",
	"autoFocus",
	"theme",
	"height",
	"minHeight",
	"maxHeight",
	"width",
	"minWidth",
	"maxWidth",
	"basicSetup",
	"placeholder",
	"indentWithTab",
	"editable",
	"readOnly",
	"root",
	"initialState"
];
var ReactCodeMirror = /* @__PURE__ */ forwardRef((props, ref) => {
	var { className, value = "", selection, extensions = [], onChange, onStatistics, onCreateEditor, onUpdate, autoFocus, theme = "light", height, minHeight, maxHeight, width, minWidth, maxWidth, basicSetup, placeholder, indentWithTab, editable, readOnly, root, initialState } = props, other = _objectWithoutPropertiesLoose(props, _excluded);
	var editor = useRef(null);
	var { state, view, container, setContainer } = useCodeMirror({
		root,
		value,
		autoFocus,
		theme,
		height,
		minHeight,
		maxHeight,
		width,
		minWidth,
		maxWidth,
		basicSetup,
		placeholder,
		indentWithTab,
		editable,
		readOnly,
		selection,
		onChange,
		onStatistics,
		onCreateEditor,
		onUpdate,
		extensions,
		initialState
	});
	useImperativeHandle(ref, () => ({
		editor: editor.current,
		state,
		view
	}), [
		editor,
		container,
		state,
		view
	]);
	var setEditorRef = useCallback((el) => {
		editor.current = el;
		setContainer(el);
	}, [setContainer]);
	if (typeof value !== "string") throw new Error("value must be typeof string but got " + typeof value);
	return /* @__PURE__ */ jsx("div", _extends({
		ref: setEditorRef,
		className: (typeof theme === "string" ? "cm-theme-" + theme : "cm-theme") + (className ? " " + className : "")
	}, other));
});
ReactCodeMirror.displayName = "CodeMirror";
//#endregion
export { ReactCodeMirror as default };

//# sourceMappingURL=index.js.map