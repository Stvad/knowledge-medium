import{readIsChildBackedWorkspace as e}from"../../data/workspaceSchema.js";import{flipWorkspaceToChildBackedProperties as t}from"../../data/workspaces.js";import{PROPERTY_CELL_BACKFILL_ID as n,countPropertyCellBackfillCandidates as r,onPropertyCellBackfillProgress as i}from"../../data/internals/propertyCellBackfill.js";import{showInfo as a,showProgress as o}from"../../utils/toast.js";import{isRemoteSyncActive as s}from"../../data/repoProvider.js";import{FolderTree as c}from"../../../node_modules/.pnpm/lucide-react@1.12.0_react@19.2.6/node_modules/lucide-react/dist/esm/icons/folder-tree.js";import{ActionContextTypes as l}from"../../shortcuts/types.js";import{openDialog as u}from"../../utils/dialogs.js";import{ConfirmMigrationDialog as d}from"./ConfirmMigrationDialog.js";var f=e=>e===void 0?``:/[.!?]$/.test(e)?e:`${e}.`,p=e=>e.undoHistoryCleared?` Undo history for this workspace was cleared.`:``,m=`This workspace was switched to property blocks — that part is done, and it applies to everyone in the workspace.`,h=(e,t,n,{flipped:r}={flipped:!1})=>{let i=g(e,t,n,r);return r?{...i,message:`${m} ${i.message}`}:i},g=(e,t,r,i)=>{let{blocksMaterialized:a,valuesMaterializedTotal:o,unmigrated:s,orphanedOwnersSwept:c}=t;switch(e.outcome){case`ran`:return o===0&&s>0?{message:`Nothing was migrated — all ${s.toLocaleString()} property value(s) failed. That is a systematic problem, not a handful of bad values; see the console before running this again.`,failed:!0}:{message:`Migrated properties on ${a.toLocaleString()} blocks.`+(c>0?` Removed the property children of ${c.toLocaleString()} block(s) whose properties had been deleted.`:``)+(r?` The workspace was edited while it ran, so some values may already be behind — run this again.`:``)+p(e),failed:!1,followUp:s>0?`${s.toLocaleString()} property value(s) could not be migrated and kept their cell value — see the console for which (first 50 shown). Repair them and run this again.`:void 0};case`deferred`:return{message:(e.undoHistoryCleared||i?`Stopped before finishing — ${f(e.reason)} Already-migrated blocks are skipped, so run it again.`:`Not started — ${f(e.reason)} Try again shortly.`)+p(e),failed:!0};case`failed`:return{message:`Stopped partway — ${f(e.reason)}${p(e)}`,failed:!0};case`held-by-peer`:return{message:`Another client holds this migration — another device, or another tab of this browser. Wait for it to finish; if nothing is running, check the claim block on the "System Migrations (km)" page and delete it to release the pass.`,failed:!0};case`already-running`:return{message:`The migration is already running on this device.`,failed:!1};case`read-only`:return{message:`This workspace is read-only, so the migration cannot write.`,failed:!0};case`not-found`:return{message:`No migration is registered under "${n}".`,failed:!0}}},_=({repo:p})=>({id:`migrate_properties_to_blocks`,description:`Migrate properties to child blocks (one-time)`,context:l.GLOBAL,icon:c,handler:async()=>{let c=p.activeWorkspaceId;if(!c)return;let l=await e(p.db,c);if(!l&&!s()){a(`This session is local-only, so the workspace cannot be switched to property blocks — that step needs remote sync.`);return}let g=await r((e,t)=>p.db.getAll(e,t),c);if(!await u(d,{blockCount:g,childBacked:l})||p.activeWorkspaceId!==c)return;let _=o(`Migrating properties to blocks…`);if(!l){let e=p.isReadOnly?`this workspace is read-only`:await p.syncViewGap();if(e!==null){_.fail(`Not started — ${f(e)} Nothing was changed; try again shortly.`);return}_.update(`Switching this workspace to property blocks…`);let n;try{({localApplied:n}=await t(p,c))}catch(e){console.error(`[properties-migration] flip failed:`,e),_.fail(`Could not switch this workspace to property blocks, so nothing was migrated: ${e instanceof Error?e.message:String(e)}`);return}if(!n){_.fail(`${m} This device has not received the workspace row yet, so the migration could not run here — run this again once sync catches up.`);return}}let v=0,y=0,b=0,x=0,S=!1,C=i(e=>{v=e.blocksMaterialized,b=e.valuesMaterializedTotal,x=e.orphanedOwnersSwept,y=e.failureCount,S=e.editedUnderPass,_.update(`Migrating properties to blocks… sweep ${e.sweeps}, ${e.blocksScanned.toLocaleString()}/${Math.max(g,e.blocksScanned).toLocaleString()}`)});try{let{message:e,failed:t,followUp:r}=h(await p.runWorkspaceBackfillNow(c,n),{blocksMaterialized:v,valuesMaterializedTotal:b,unmigrated:y,orphanedOwnersSwept:x},S,{flipped:!l});t?_.fail(e):_.done(e),r&&a(r,{id:`properties-migration-worklist`,duration:1/0})}catch(e){console.error(`[properties-migration] failed:`,e),_.fail(`Migration failed: ${e instanceof Error?e.message:String(e)}`)}finally{C()}}});export{h as describeOutcome,_ as migratePropertiesToBlocksAction};
//# sourceMappingURL=action.js.map