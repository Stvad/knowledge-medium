import{readIsChildBackedWorkspace as e}from"../../data/workspaceSchema.js";import{PROPERTY_CELL_BACKFILL_ID as t,countPropertyCellBackfillCandidates as n,onPropertyCellBackfillProgress as r}from"../../data/internals/propertyCellBackfill.js";import{showInfo as i,showProgress as a}from"../../utils/toast.js";import{FolderTree as o}from"../../../node_modules/.pnpm/lucide-react@1.12.0_react@19.2.6/node_modules/lucide-react/dist/esm/icons/folder-tree.js";import{ActionContextTypes as s}from"../../shortcuts/types.js";import{openDialog as c}from"../../utils/dialogs.js";import{ConfirmMigrationDialog as l}from"./ConfirmMigrationDialog.js";var u=(e,n,r)=>{switch(e.outcome){case`ran`:return{message:`Migrated properties on ${n.toLocaleString()} blocks.`+(e.undoHistoryCleared?` Undo history for this workspace was cleared.`:``),failed:!1,followUp:r>0?`${r.toLocaleString()} property value(s) could not be migrated and kept their cell value — see the console for which. Repair them and run this again before flipping the workspace.`:void 0};case`deferred`:return{message:`Not started — ${e.reason}. Try again shortly.`,failed:!0};case`failed`:return{message:`Stopped partway — ${e.reason} Run it again; already-migrated blocks are skipped.`,failed:!0};case`held-by-peer`:return{message:`Another device holds this migration. Wait for it to finish — or, if that device is gone, delete the claim block on the "System Migrations (km)" page and run this again.`,failed:!0};case`already-running`:return{message:`The migration is already running on this device.`,failed:!1};case`read-only`:return{message:`This workspace is read-only, so the migration cannot write.`,failed:!0};case`not-found`:return{message:`No migration is registered under "${t}".`,failed:!0}}},d=({repo:d})=>({id:`migrate_properties_to_blocks`,description:`Migrate properties to child blocks (one-time)`,context:s.GLOBAL,icon:o,handler:async()=>{let o=d.activeWorkspaceId;if(!o)return;if(await e(d.db,o)){i(`This workspace already reads properties from child blocks — the migration has nothing left to do.`);return}let s=await n((e,t)=>d.db.getAll(e,t),o);if(!await c(l,{blockCount:s})||d.activeWorkspaceId!==o)return;let f=a(`Migrating properties to blocks…`),p=0,m=0,h=r(e=>{p=e.blocksMaterialized,m=e.failureCount,f.update(`Migrating properties to blocks… sweep ${e.sweeps}, ${e.blocksScanned.toLocaleString()}/${Math.max(s,e.blocksScanned).toLocaleString()}`)});try{let{message:e,failed:n,followUp:r}=u(await d.runWorkspaceBackfillNow(o,t),p,m);n?f.fail(e):f.done(e),r&&i(r,{duration:1/0})}catch(e){console.error(`[properties-migration] failed:`,e),f.fail(`Migration failed: ${e instanceof Error?e.message:String(e)}`)}finally{h()}}});export{d as migratePropertiesToBlocksAction};
//# sourceMappingURL=action.js.map