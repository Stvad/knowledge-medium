import{OBJECT_BAG as e,keyOf as t,scanPropertyKeys as n}from"../../data/internals/propertyKeyScan.js";var r=50,i=25,a=3,o="Nothing declares this name — no definition block, no code seed — so property migration skips it (propertyChildrenProcessor.ts: `resolveNameSchema` → `continue`) and it is the one class of property data a flipped workspace cannot make child-backed. Fix IN THIS ORDER: (1) if an extension owns the key, install / enable it so its seed materializes the definition; (2) only then let §9 orphan synthesis mint a user-origin definition. Synthesizing first and enabling the owner later collides in the winner machinery and strands field rows on the loser's fieldId. Registering is additive — no cell value is rewritten.",s="A definition block exists for this name but the workspace cannot build behavior from it, so it registers no schema and the name resolves to nothing. Check IN THIS ORDER: (1) is its preset contributed by an extension that is not installed / enabled / loaded here? `tryBuildSchema` returns nothing for an unknown preset id, and the definition is then perfectly valid — enabling the provider fixes it, while editing the block would destroy a working preset config; (2) only if the provider IS loaded, repair the block itself (config the codec rejects, or metadata that fails to parse). Either way do not add a second definition for this name — it would collide.",c=`The empty property key is a hard flip blocker: no definition can ever back it. Delete or remap it before any workspace flips to child-backed properties (§9).`,l=e=>e.property===``?{fix:c,blocksFlip:!0}:e.reason===`ambiguous`?{fix:"Unreachable via a name lookup (see describeUnregisteredProperty). Two code seeds declaring one name would leave only the first in the registry; namespace one of them (`myplugin:name`)."}:e.reason===`shadowed`?{fix:`Unreachable via a name lookup (see describeUnregisteredProperty). A non-winning definition block for this name would be shadowed by the winner; retire or rename the loser rather than adding another definition.`}:{fix:e.definitionBlocks>0?s:o},u=e=>{if(!e)return[];try{let t=JSON.parse(e);return Array.isArray(t)?t.filter(e=>typeof e==`string`):[]}catch{return[]}},d=async(e,t,r={})=>{let i=await n(e,t),a=i.unresolved.map(e=>({...e,...l(e),types:[],sampleBlockIds:[]}));return await f(e,t,a,{keys:Math.max(0,Math.floor(r.keys??50)),blocksPerKey:Math.max(1,Math.floor(r.blocksPerKey??25))}),{workspaceId:t,syncGap:i.syncGap,syncedThrough:e.lastSyncedAt?.toISOString()??null,distinctProperties:i.distinctProperties,propertyCells:i.propertyCells,registeredProperties:i.registeredProperties,unregistered:a,unregisteredCells:a.reduce((e,t)=>e+t.cells,0),unreadableBlocks:i.unreadableBlocks}},f=async(n,r,i,o)=>{let s=i.slice(0,o.keys);for(let e of i.slice(o.keys))e.provenanceOmitted=!0;if(s.length===0)return;let c=await n.db.getAll(`SELECT ranked.property AS property,
            ranked.blockId AS blockId,
            (SELECT json_group_array(bt.type) FROM block_types bt
              WHERE bt.block_id = ranked.blockId AND bt.workspace_id = ?) AS types
       FROM (
         SELECT property, blockId,
                ROW_NUMBER() OVER (PARTITION BY property ORDER BY blockId) AS rn
           FROM (
             -- DISTINCT because a stored bag CAN repeat a key: JSON.stringify
             -- cannot produce one, but a raw SQL write can, and the types
             -- trigger sees only one row so nothing rejects it. Without this,
             -- json_each emits a row per occurrence and one block could eat
             -- the whole per-key cap: repeating in sampleBlockIds, inflating
             -- sampledBlocks, and hiding the blocks that actually differ.
             SELECT DISTINCT j.key AS property, b.id AS blockId
               FROM blocks b, json_each(${e}) j
              WHERE b.workspace_id = ? AND b.deleted = 0
                AND j.key IN (SELECT value FROM json_each(?))
           )
       ) ranked
      WHERE ranked.rn <= ?`,[r,r,JSON.stringify(s.map(e=>e.property)),o.blocksPerKey]),l=new Map,d=new Map;for(let e of c){let n=t(e.property),r=d.get(n)??[];r.length<a&&(r.push(e.blockId),d.set(n,r));let i=l.get(n)??new Map;for(let t of u(e.types))i.set(t,(i.get(t)??0)+1);l.set(n,i)}for(let e of s)e.sampleBlockIds=d.get(e.property)??[],e.types=[...l.get(e.property)??new Map].map(([e,t])=>({type:e,sampledBlocks:t})).sort((e,t)=>t.sampledBlocks-e.sampledBlocks||e.type.localeCompare(t.type))};export{i as PROVENANCE_BLOCKS_PER_KEY,r as PROVENANCE_KEY_LIMIT,d as auditPropertyRegistration,l as describeUnregisteredProperty};
//# sourceMappingURL=propertyRegistrationAudit.js.map