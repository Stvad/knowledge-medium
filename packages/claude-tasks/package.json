{
  "name": "@knowledge-medium/claude-tasks",
  "private": true,
  "version": "0.1.0",
  "description": "Trigger Claude Code from the notes graph: [[claude]] mention tasks, backlink watchers, and query-change watchers. Local daemon + MCP server over the kmagent bridge; runs bill to the Claude subscription (claude -p), not the API.",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "bin": {
    "km-claude-daemon": "./dist/daemon.js",
    "km-mcp": "./dist/mcp.js"
  },
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@knowledge-medium/agent-cli": "^0.2.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.0.0",
    "typescript": "^6.0.0"
  }
}
