// Hand-written declarations for pr-reply.mjs (runtime must stay plain
// node-runnable JS — it runs as a pnpm script with no loader).
export declare const SIGNATURE: string
export declare const signedBody: (text: string) => string
