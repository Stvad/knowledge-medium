// Hand-written declarations for is-main-module.mjs (runtime must stay plain
// node-runnable JS — hook scripts import it with no loader).
export declare const isMainModule: (metaUrl: string) => boolean
