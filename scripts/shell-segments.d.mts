// Hand-written declarations for shell-segments.mjs (runtime must stay plain
// node-runnable JS — it is imported by Claude Code hooks with no loader).
export declare const shellSegments: (cmd: string) => string[][]
